let arr = [1, 2, 3, 4, 5];
arr.map((num) => num * 2); // returns a new array with each element multiplied by 2
console.log(arr); // Output: [1, 2, 3, 4, 5]

const newArr = arr.map((num) => num * 2);
console.log(newArr); // Output: [2, 4, 6, 8
