// add 2 for all array elements using map
const numbers = [1, 2, 3, 4, 5];
const incrementedNumbers = numbers.filter((num) => num % 2 == 0);
console.log(incrementedNumbers); // Output: [3, 4, 5, 6, 7]
